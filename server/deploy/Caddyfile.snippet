# Append to /etc/caddy/Caddyfile. Replace 1-2-3-4 with the server IP (dashes).
# Caddy gets a Let's Encrypt certificate for the sslip.io name automatically.
1-2-3-4.sslip.io {
	reverse_proxy 127.0.0.1:3100
}
